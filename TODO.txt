- write ember code injections

- Check swc and ratel transpilers
- write all babel addons
- compiles hbs templates

swc Compiler high-level API is private inside node-swc/native

fm = c.cm.new_source_file(FileName::Anon || FileName::Real(options.filename.clone().into(), source.value())

self.c.process_js_file(self.fm.clone(), self.options.clone())

